<section class="container my-5">

  <div class="row">
    <div class="col-md-4 col-sm-12">
      <figure class="abaout__img">
        <h2 class="subtitle text-center"><%= @master.user.first_name.upcase%> <%= @master.user.last_name.upcase %></h2>
        <%= cl_image_tag @master.user.photo.key, height: 217, width: 217, crop: :fill, class: "rounded-circle"%>
      </figure>
    </div>

    <div class="col-md-4 col-sm-12">
      <div class="abaout__texts">
        <h2 class="subtitle text-center">
          Descripción del Mastero
        </h2>
        <p class="abaout__paragraph text-justify">
         <%= @master.description %>
        </p>
        <button type="button" class="btn btn-warning rounded-pill" id="boton-contratar">CONTRATAR MAESTRO</button>
      </div>
    </div>

    <div class="col-md-4 col-sm-12">
      <figure class="map__img">
        <div id="map"
              style="width: 100%; height: 300px;"
              data-markers="<%= @markers.to_json %>"
              data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"></div>
      </figure>
    </div>
  </div>

</section>

<section class="projects">

 <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
      <ol class="carousel-indicators">
        <% @master.photos.each_with_index do |photo, index|%>
          <% if index == 0 %>
            <li data-target="#carouselExampleIndicators" data-slide-to="0" class="active"></li>
          <% else %>
            <li data-target="#carouselExampleIndicators" data-slide-to="<%= index %>"></li>
          <% end %>
        <% end %>
      </ol>
  <div class="carousel-inner">
    <% @master.photos.each_with_index do |photo, index|%>
      <% if index == 0 %>
        <div class="carousel-item active">
      <% else %>
        <div class="carousel-item">
      <% end %>
        <h1 class="text-banner">Trabajos Realizados</h1>
        <%= cl_image_tag photo.key, height: 900, width: 900, crop: :fill, class: "d-block w-100", id: "imagencarrusel"%>
      </div>
    <% end%>
  </div>
      <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="sr-only">Previous</span>
      </a>
      <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="sr-only">Next</span>
      </a>
  </div>

</section>
